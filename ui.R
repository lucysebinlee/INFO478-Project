# Load Packages
library(shiny)
library(ggplot2)
library(dplyr)
library(plotly)
library(DT)

# Define UI for application
shinyUI(
  navbarPage(
    "Student Alcohol Consumption",
    tabPanel(
      mainPanel(
      )
    ),
    tabPanel(
      mainPanel(
      )
    )
  )
)